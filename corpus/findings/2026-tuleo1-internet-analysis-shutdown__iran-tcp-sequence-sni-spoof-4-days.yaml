# Auto-extracted by corpus-findings from full paper text.
# Review summary + tags before relying.
paper: 2026-tuleo1-internet-analysis-shutdown
kind: defense
summary: 'Two TCP-layer SNI-spoofing techniques briefly circumvented Iran''s DPI for approximately four days: (1) sending a fake ClientHello with a TTL too low to reach the server but high enough to pass the censor, and (2) sending a fake ClientHello at a future TCP sequence number that the server would ignore but the stateless DPI would accept. Both were defeated once Iran placed Cloudflare IPs behind a stateful NAT that validated checksums and sequence numbers.'
techniques:
    - dpi
    - sni-blocking
    - middlebox-interference
defenses:
    - geneva
censors:
    - ir
defense_implications:
    - TTL-limited and out-of-order fake-record attacks on stateless DPI are viable only until the censor deploys stateful reassembly; treat them as a temporary emergency measure rather than a durable transport design.
    - Publish such techniques only when active deployment is already widespread — the 4-day window before countermeasure suggests that mass adoption itself triggers rapid patching.
section: Issue body (SNI spoofing methods paragraph)
extracted_by: claude-sonnet-4-6
