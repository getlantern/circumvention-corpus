# Auto-extracted by corpus-findings from full paper text.
# Review summary + tags before relying.
paper: 2025-its0ka-russia-mobile-network
kind: detection
summary: 'Russian mobile carrier Beeline deployed an IP-level whitelist during regional network shutdowns: TCP SYN packets reach the remote host but TLS ClientHello is silently dropped for any non-whitelisted IP. Any SNI value succeeds when connecting to a whitelisted IP (e.g., Yandex), while no SNI value succeeds on non-whitelisted IPs, confirming the filter operates at the IP-address layer rather than the DNS or SNI layer.'
techniques:
    - ip-blocking
    - dpi
    - sni-blocking
censors:
    - ru
defense_implications:
    - SNI manipulation (ECH, domain fronting, SNI hiding) will not bypass this blocking layer — the proxy server's IP address must itself be whitelisted or indistinguishable from a whitelisted range.
    - 'Prioritize IP camouflage strategies: co-locate exit nodes on whitelisted IP ranges (e.g., Yandex AS) or use tunneling that causes traffic to appear to originate from an allowed IP block.'
section: Issue body (initial report)
extracted_by: claude-sonnet-4-6
