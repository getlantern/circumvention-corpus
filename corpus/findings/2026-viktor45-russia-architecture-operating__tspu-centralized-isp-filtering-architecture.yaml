# Auto-extracted by corpus-findings from full paper text.
# Review summary + tags before relying.
paper: 2026-viktor45-russia-architecture-operating
kind: deployment
summary: Russia's TSPU (Targeted System for Device Filtering and Redirection) is the centralized filtering infrastructure mandated at ISP level, documented in technical materials from a 2021 presentation. TSPU sits in-path on ISP networks and performs filtering and traffic redirection rather than relying solely on endpoint-level DNS.
techniques:
    - dpi
    - ip-blocking
censors:
    - ru
defense_implications:
    - TSPU is in-path (not DNS-only), so DNS-level circumvention alone is insufficient — the transport layer itself must be obfuscated.
    - Because filtering is mandated at all ISPs, there is no 'less-filtered' Russian ISP to route through; designs must assume uniform deployment.
section: Abstract / presentation summary
extracted_by: claude-sonnet-4-6
