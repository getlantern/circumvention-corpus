# Auto-extracted by corpus-findings from full paper text.
# Review summary + tags before relying.
paper: 2026-pooribitwise-iran-advanced-dpi
kind: detection
summary: 'Blocking by Iran''s DPI manifests as two simultaneous mechanisms: DNS hijacking that redirects queries to 10.10.34.34 (a known blackhole IP), and RST floods on direct connections using the blocked SNI against the CDN IP, indicating both DNS-layer and TCP-layer enforcement are applied in tandem.'
techniques:
    - dns-poisoning
    - rst-injection
    - sni-blocking
censors:
    - ir
defense_implications:
    - 'Clients must bypass both DNS and TCP RST layers: DoH/DoT or hardcoded IPs address DNS hijacking, but the RST flood fires on any connection carrying the blocked SNI regardless of how the IP was resolved — ECH or a different SNI strategy is required at the TLS layer.'
    - A single blocked domain name triggers both mechanisms; rotate domain names aggressively and avoid reusing root domains across clients.
section: The Issue
extracted_by: claude-sonnet-4-6
