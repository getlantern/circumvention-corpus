# Auto-extracted by corpus-findings from full paper text.
# Review summary + tags before relying.
paper: 2022-huangshaomings001-google-auto-jump
kind: defense
summary: Appending the /ncr (No Country Redirect) parameter to Google URLs is reported as a workaround that prevents the forced redirect to google.cn/m. This suggests the redirect is partially under application-layer control rather than being enforced purely at the network level.
defenses:
    - mimicry
censors:
    - cn
defense_implications:
    - Circumvention tools could transparently rewrite google.com requests to include /ncr (or equivalent query parameters) to prevent destination-side cooperation with censorship-adjacent redirects.
    - The effectiveness of /ncr suggests that at least some of the redirect is Google-side geolocation logic rather than GFW injection—tools should treat these as distinct failure modes requiring distinct mitigations.
section: Issue thread discussion
extracted_by: claude-sonnet-4-6
